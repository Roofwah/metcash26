# Leave blank to use same-origin API (recommended in production)
REACT_APP_API_URL=
# Prevent CRA nested-workspace preflight conflicts
SKIP_PREFLIGHT_CHECK=true
# Disable CRA eslint plugin in nested repo contexts
DISABLE_ESLINT_PLUGIN=true
